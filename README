This is web deployable Blackjack game. To play through local host follow these steps.

1. From the command line, you can start the server by 'ruby main.rb'
2. If you have the 'shotgun' gem installed, you can instead run 'shotgun main.rb'
3. ctrl+c to stop the server
